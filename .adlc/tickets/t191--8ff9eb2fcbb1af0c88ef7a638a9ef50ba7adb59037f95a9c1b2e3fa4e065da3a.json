{
  "body": "FOUND DURING T186's curse slice, 2026-08-19. Criterion 11 itself is MET and green: cursing one bound\nlaw retires that law only, and the retirement survives a subsequent poll. This ticket is the piece\nBEYOND the criterion that I added and could not finish.\n\nWHAT DOES NOT WORK: `curseChannelLaw(channel, living, { lift: true })`. It strikes the curse record\nusing the substrate's real negation shape (makeNegationClaims — an earlier draft hand-rolled a\n\"negates\" pointer, which appended cleanly and changed nothing, and that bug IS fixed). The next poll\nstill does not re-bless.\n\nTHE EVIDENCE, and it narrows the search: the living name is never re-attempted at all. `parked` comes\nback EMPTY, so the blessing is not refusing — the skip is happening upstream, in the cursed-set\nfilter. Either the strike is not binding (an authority question) or `cursesOf` is reading a stale\nset. Diagnosed no further; the loop moved on rather than burn an iteration on a nice-to-have.\n\nWHY IT MATTERS ANYWAY: a curse the operator cannot lift is a one-way door on a REVERSIBLE control.\n§46's whole toggle design separates reversible acts (freeze, unbless, curse) from the irreversible one\n(drop). A curse that cannot be undone quietly joins the wrong group, and an operator who learns that\nthe hard way has lost a peer's law with only a re-federation to recover it.\n\nRAILS: the skipped rail in test/federation/curse-durable.test.ts is written and ready — un-skip it.\nTwo-sided: lifting a curse on one law must not lift it on a channel-mate that is also cursed.\n\nDO NOT ship `lift` in any doc, help text, or tool description as working until that rail is green.\n\nROOT CAUSE FOUND by the P5 suppression lens, 2026-08-19, and it is not where I was looking.\n\nLifting negates the CURSE RECORD and stops there. The negation the curse appended over the\nREGISTRATION BINDING is left standing. The lift comment claims \"the next poll re-blesses through the\nordinary path, so nothing here needs to know how binding works\" — that is false by construction:\n\nadoptLaw INHERITS THE SOURCE TIMESTAMPS, and a delta id hashes {author, pointers, timestamp} (H4). So\nthe re-published binding is byte-identical to the one that was struck — THE SAME ID, with the\noperator's negation still pointing at it. It is born struck. Nothing in the lift path removes that\nnegation, and nothing reports the disagreement: the curse record reads lifted while the ground still\nsays retired.\n\nTHE FIX IS ON THE STRIKE, NOT THE RECORD: lifting must negate the negation the curse appended over\nthe binding. That also explains the symptom recorded above — the name looked \"never re-attempted\"\nbecause re-attempting it produces a delta that is already dead.\n\nA RELATED NON-TRANSITIVITY, same family, worth fixing in the same pass: cursesOf and\nchannelStatusImpl both test negationsOf(d.id).length > 0. That is PRESENCE of a strike, not SURVIVAL\nof one. A struck strike revives under rhizomatic's algebra but not under this test, so a curse whose\nlift was itself struck stays invisible and the standing sync re-blesses retired law. Reachable only\nthrough a hand-appended negation today, but it is H1's \"one link is not enough\" clause exactly.\n",
  "edges": [],
  "id": "T191",
  "phase": "P0",
  "rails": [],
  "scope": [
    "src/federation/channel.ts"
  ],
  "status": "todo",
  "title": "Lifting a curse does not re-bless — the cursed name is never re-attempted"
}
