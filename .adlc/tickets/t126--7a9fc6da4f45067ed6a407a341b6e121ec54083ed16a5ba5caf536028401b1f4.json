{
  "body": "PHASE 5 OF FIFTEEN. The plan is `.adlc/specs/users-oauth-phasing-plan.md`, approved by Myk\n2026-07-27. It replaces #282, which held 13,299 lines in one pull request that Myk could not read.\n\nTHIS TICKET IS ONE PULL REQUEST TO `main`. It merges on its own and it is useful on its own.\n\nWHO MERGES IT: MYK. It decides something rather than repairing something — see the plan's §9c.\n\nSPEC SECTION: §36. Per the plan's §9d, fifteen phases group into two sections. The FIRST phase of a\ngroup writes `spec/NN-slug.md`; every later phase EDITS it. Add one `{says, spec, proof}` claim to\nthis group's chapter in `demos/capabilities/chapters.mjs`, or `test/site/capabilities.test.ts` stays red.\n\nRAILS ARE EMPTY BY DESIGN — read the plan's §9e. CLAUDE.md says declare rails when the tests EXIST,\nnever in advance. A rail declared early binds two tickets together, which is exactly how #282 became one\nchange. Declare this ticket's rails at P3, once the tests are written and RED, using\n`adlc ticket update`. The rail files named below are SCOPE.\n\nTHE TWO RULES THIS PHASE OBEYS, from the plan's §1:\n  (i) it owns its own rail FILES and shares none with another phase;\n  (ii) it adds no PRECONDITION to a door an earlier phase railed.\n\nTHE SETTLED DECISIONS behind this work, so no phase reopens one:\n  - Role assignment is CLI-ONLY (Myk, 2026-07-27). The filesystem is the trust root; anyone who can read\n    `<home>/operator.seed` can already sign as the store. Plan §9.\n  - EVERY OPERATOR IS EQUIVALENT over the whole store (Myk, 2026-07-27). There are no operator tiers.\n    Per-operator keys buy ATTRIBUTION, never privilege separation. Plan §9a.\n  - `remove-role` binds against a REMOTE user only. It does not bind against home access. Do not claim\n    otherwise in any message or help text — that is the H7 shape. Plan §9a.\n  - Roles are a SET of non-negated claims, never last-write-wins (Myk).\n  - The login limiter DELAYS; it never denies and never locks (Myk, option b).\n\nWHAT THE REVIEW ROUNDS TAUGHT, and every criterion below carries one of these lessons rather than\nrediscovering it on round six. The full list is the plan's §2:\n  - The defect sits in the ADJACENT line. Review a fix by reading what moved beside it.\n  - A rail written from a model agrees with BOTH versions. Revert the fix and watch the rail fail.\n  - A rail of only negatives passes on an unrelated answer. Every negative needs a positive control.\n  - An instrument that cannot fail is worse than a rail that cannot fail. Make a probe report a\n    KNOWN-wrong answer before trusting it.\n  - Grep-verify any claim that names a file, a marker, a count or a command output.\n\n--- THE PHASE, VERBATIM FROM THE PLAN ---\n\n### Phase 5 — The login door\n\n**Verification.** Every criterion below is proved in `test/server/login-door.test.ts`. A criterion\nthat cannot name an assertion in that file is not ready to build.\n\n**Delivers.** `GET /login`, `POST /login`, `POST /logout`. The session cookie. The pre-session\ncookie.\n\n**Merges alone.** An operator can sign in. The session opens no data door yet.\n\n**Must not.** It must not open a JSON door. Phase 7 does that, through a bearer token.\n\n**IT SHIPS THE WHOLE FORM-TOKEN SURFACE, and phase 6 adds only the ENFORCEMENT.** This is clause (ii)\nof section 1's rule. If phase 5 railed a `POST /login` with no token field, phase 6 could not make the\ntoken mandatory without editing phase 5's frozen rail file. So phase 5 issues the token on `GET /login`,\naccepts it on `POST /login`, and **every phase 5 rail sends it**. Phase 5 does not yet refuse a request\nthat omits it. Phase 6 changes only what the door REFUSES.\n\n**Criteria from findings.**\n0. `GET /login` issues a form token and the page carries it. `POST /login` accepts it and ignores its\n   absence. Every rail in this file sends a valid token, so phase 6 can turn absence into a refusal\n   without touching one assertion here.\n1. THE PRE-SESSION NONCE USES ITS OWN COOKIE. `GET /login` sets a nonce whenever no session is\n   presented. `SameSite=Lax` presents none on a cross-site subresource request. One shared name\n   therefore lets any page sign the operator out. The orphaned session keeps its idle window with no\n   cookie to reach it, so its tokens become unrevokable.\n2. A cross-site `GET /login` does not set the session cookie. The live session still mints.\n3. The cookie attribute string is one pinned literal: `HttpOnly; Secure; SameSite=Lax; Path=/`. No\n   `Domain`. Assert it byte for byte.\n4. That string is identical under `X-Forwarded-Proto`, a foreign `Host`, and no forwarding header.\n5. Exactly ONE session cookie is set. A reader that takes the first match cannot see a second.\n6. Logging in over a live session mints a DIFFERENT id. The old id opens nothing.\n7. A wrong password, an unknown user and a missing role answer the same status and the same body.\n8. The login page carries a `Content-Security-Policy` that permits no script. The page carries no\n   script.\n9. A request with no credential receives the bytes it received before §36 existed. The refusal never\n   reveals that users exist.\n10. THE PUBLIC URL IS CONFIGURED. `--public-url` names the outside address. `Host` and\n    `X-Forwarded-*` are the caller's to write and change nothing.\n\n--- END ---\n\nP1 DELIVERABLE: a working spec at `.adlc/specs/36-05-the-login-door.md`. EVERY criterion above must name its\nverification — a test path or a backtick command — or `spec-lint` fails it as a wish. The plan itself\nfails spec-lint with 110 wishes ON PURPOSE (plan §9b): it is a decomposition, and these fifteen working\nspecs are the gated artifacts. Transcription, not invention.\n",
  "edges": [
    {
      "contract": "Phase 6 turns a missing form token into a refusal. Phase 5 ships the token surface and sends it in every rail, which is what lets phase 6 add the precondition without editing phase 5's frozen rail file — the plan's rule (ii).",
      "to": "T127"
    },
    {
      "contract": "The delay is paid at the login door, before the password comparison. The door must exist first.",
      "to": "T130"
    },
    {
      "contract": "Erasure honesty covers what a user's deltas and session rows leave behind. Both arrive with the door.",
      "to": "T131"
    }
  ],
  "id": "T126",
  "phase": "P1",
  "rails": [],
  "scope": [
    "src/server/session.ts",
    "src/gateway/http.ts",
    "test/server/login-door.test.ts",
    ".adlc/specs/36-05-the-login-door.md"
  ],
  "status": "todo",
  "title": "§36 phase 5/15 — The login door"
}
