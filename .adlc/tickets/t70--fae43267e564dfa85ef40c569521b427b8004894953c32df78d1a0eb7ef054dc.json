{
  "body": "HIGH, erasure-integrity (H7/H9). Found by the loam-erasure lens during T67's P5 (2026-07-23). RE-SCOPED 2026-07-24 after T67 landed: part (a)'s SURFACING is already done — cli.ts (the boot heal) now loops `healed.purgeFailures` and prints a loud stderr warning per refusal, with the H9 rationale (cli.ts ~146-152). What T67 did NOT do, and this ticket now covers:\n\n(b) HEAL'S OWN BYTE VERDICT. `heal` still trusts `purge`'s return: a sweep that returns a count and does not throw is taken as done. That is the exact §11 conflation T40 caught elsewhere — a purge can report success while bytes sit at rest (a freelist page, a .tmp straggler, a WAL image the count never saw). `erase` was converted to a `holds`-based verdict (survivingTombstones); heal was not. FIX: after the sweep, ask each tier `holds(id)` for every dead id and record any survivors in HealReport (e.g. `survivingPrimary`/`survivingMirror`, ids that a purge claimed yet a tier still holds). Counts stay as EVIDENCE OF WORK; the holds answer is the VERDICT.\n\n(a-remainder) DEGRADED LATCH. Surfacing to stderr is necessary but ephemeral. A refused-or-incomplete sweep should LATCH a durable degraded signal rather than let serve continue as if clean — the store keeps serving (refusing to boot trades a leak for an outage, per the existing comment), but the degraded state is observable (a flag on the report the boot path acts on; do not silently swallow). Keep it a REPAIR: surface + latch, do not refuse boot.\n\nRAILS (P3). (1) Drive a tier whose `purge` REFUSES (throws) through heal and assert `purgeFailures` names it AND the boot path tells the operator (object level: assert what `serve`/io.err actually writes, not just the struct) — the catch branch at mirror.ts is entered by NO existing test today (`purgeFailures` appears only as `[]`). (2) Drive a tier whose `purge` RETURNS SUCCESS but leaves the id `holds`-true, and assert heal's new verdict reports the survivor — object level, not the count. Assert at BOTH levels (delta/count AND holds/serve output) per the P3 discipline.\n\nScope: src/store/mirror.ts (heal + HealReport), src/cli/cli.ts (boot heal reads the verdict + latch), test/store/mirror.test.ts. Evidence: T67 P5 loam-erasure finding; SUBSTRATE-HAZARDS H7/H9.",
  "edges": [],
  "id": "T70",
  "phase": "P1",
  "rails": [],
  "scope": [
    "src/store/mirror.ts",
    "src/cli/cli.ts",
    "test/store/mirror.test.ts"
  ],
  "status": "todo",
  "title": "The boot sweep can fail in total silence — heal's purgeFailures has no reader"
}
