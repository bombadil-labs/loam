{
  "_adlcArchive": {
    "archivedAt": "2026-07-28T12:29:36.532Z",
    "reason": "completed",
    "sourceRevision": null,
    "sourceStoreHash": "44fb8622aa4d5c54252e6c762b7d8207b7e5d923b39858b4fffd35af0e5f262d",
    "ticketHash": "51511505091f0d1826908e0007d5c486d328db90b97f8319912a6245683ff714",
    "version": 1
  },
  "body": "PHASE 2 OF FIFTEEN. The plan is `.adlc/specs/users-oauth-phasing-plan.md`, approved by Myk\n2026-07-27. It replaces #282, which held 13,299 lines in one pull request that Myk could not read.\n\nTHIS TICKET IS ONE PULL REQUEST TO `main`. It merges on its own and it is useful on its own.\n\nWHO MERGES IT: SELF-MERGE on the ordinary bar: `npm run check` green, P5 clean, post-work audit clean. It implements a decision Myk already settled. Plan §9c.\n\nSPEC SECTION: §36. Per the plan's §9d, fifteen phases group into two sections. The FIRST phase of a\ngroup writes `spec/NN-slug.md`; every later phase EDITS it. Add one `{says, spec, proof}` claim to\nthis group's chapter in `demos/capabilities/chapters.mjs`, or `test/site/capabilities.test.ts` stays red.\n\nRAILS ARE EMPTY BY DESIGN — read the plan's §9e. CLAUDE.md says declare rails when the tests EXIST,\nnever in advance. A rail declared early binds two tickets together, which is exactly how #282 became one\nchange. Declare this ticket's rails at P3, once the tests are written and RED, using\n`adlc ticket update`. The rail files named below are SCOPE.\n\nTHE TWO RULES THIS PHASE OBEYS, from the plan's §1:\n  (i) it owns its own rail FILES and shares none with another phase;\n  (ii) it adds no PRECONDITION to a door an earlier phase railed.\n\nTHE SETTLED DECISIONS behind this work, so no phase reopens one:\n  - Role assignment is CLI-ONLY (Myk, 2026-07-27). The filesystem is the trust root; anyone who can read\n    `<home>/operator.seed` can already sign as the store. Plan §9.\n  - EVERY OPERATOR IS EQUIVALENT over the whole store (Myk, 2026-07-27). There are no operator tiers.\n    Per-operator keys buy ATTRIBUTION, never privilege separation. Plan §9a.\n  - `remove-role` binds against a REMOTE user only. It does not bind against home access. Do not claim\n    otherwise in any message or help text — that is the H7 shape. Plan §9a.\n  - Roles are a SET of non-negated claims, never last-write-wins (Myk).\n  - The login limiter DELAYS; it never denies and never locks (Myk, option b).\n\nWHAT THE REVIEW ROUNDS TAUGHT, and every criterion below carries one of these lessons rather than\nrediscovering it on round six. The full list is the plan's §2:\n  - The defect sits in the ADJACENT line. Review a fix by reading what moved beside it.\n  - A rail written from a model agrees with BOTH versions. Revert the fix and watch the rail fail.\n  - A rail of only negatives passes on an unrelated answer. Every negative needs a positive control.\n  - An instrument that cannot fail is worse than a rail that cannot fail. Make a probe report a\n    KNOWN-wrong answer before trusting it.\n  - Grep-verify any claim that names a file, a marker, a count or a command output.\n\n--- THE PHASE, VERBATIM FROM THE PLAN ---\n\n### Phase 2 — A user is a fact\n\n**Verification.** Every criterion below is proved in `test/server/users-ground.test.ts`. A criterion\nthat cannot name an assertion in that file is not ready to build.\n\n**Delivers.** A user record and a role binding, both as claims in the ground. A HyperSchema that\ncounts the operator's assertions only. A Schema that resolves latest-wins. `resolveUserView` and\n`rolesOf`, which returns the SET of roles a user holds.\n\n**Merges alone.** Users become readable facts. No door consumes them yet.\n\n**Must not.** It must not touch `credentials.json`. It must not add a door.\n\n**Criteria from findings.**\n1. A user is an ENTITY. Its properties resolve through a Schema. Say so in the prose. (#282's header\n   said \"the half of a user that IS a delta\", which collapses a claim into a fact. Myk caught it.)\n2. \"Operator-signed\" is enforced on the READ, not asserted on the write. Any author with write\n   standing may sign a claim at an ordinary context. So the select names the operator.\n3. A stranger's negation does not retract what the operator said. Assert at both levels.\n4. A store with no operator yields no user and no role. The door stays shut.\n5. A struck role binding leaves the user readable and that role absent from the set. Assert both\n   levels.\n6. **A USER HOLDS MANY ROLES. THE PROPERTY IS A SET, NOT A LATEST VALUE.** The Policy for the role\n   context is `all`, never `pick`. So the resolved value is every non-negated role claim, and two roles\n   coexist. Rail it: grant `operator` and `actor` to one user, and both resolve. `pickLatest` would let\n   the second grant silently displace the first, which is a permission bug wearing a data model's\n   clothes.\n7. A user with no grants resolves to an EMPTY SET, not to `undefined` and not to a default role. A\n   permission check asks MEMBERSHIP — does this user hold `operator` — never equality.\n8. The reading function is `rolesOf`, and it returns a set. There is no `roleOf` returning one role.\n   A singular name would invite the caller to compare rather than to test membership.\n9. A user name is safe in an entity id, a JSON key and an HTML page. One expression, stated once.\n10. **MANY USERS MAY HOLD THE OPERATOR ROLE.** Each user is its own entity, and its role binding is a\n   claim at that entity. So the shape carries no limit of one. Rail it: assign the role to two users,\n   and `rolesOf` contains `operator` for both. Rail the other side: revoking one leaves the other.\n11. **THE ROLE READ NAMES THE STORE'S OWN SEED AND NOTHING ELSE.** It stays exactly as it is today:\n   `authoredBy: <the key in `<home>/operator.seed`>`. Do not widen it to a trust set. Revision 1 did,\n   and that was an escalation — `users.ts` states that nothing refuses a role claim at the append door,\n   so the read-side select is the ONLY defence.\n   **Name it the STORE'S seed, never \"the genesis operator's\".** That phrasing implies a senior operator,\n   and §9a says there is no such thing. The seed belongs to the store. Every operator with home access\n   uses the same one.\n12. **A ROLE CLAIM SIGNED BY ANY OTHER KEY RESOLVES TO NOTHING.** This is the rail that proves the\n   escalation is closed, and it is two-sided. Append a role claim at `user:carol` signed by a key that\n   is NOT the store's seed, and `rolesOf(\"carol\")` is empty. Append the same claim signed by the store's\n   seed, and it contains `operator`. Assert at both levels: the stray delta IS in the store, and no\n   reading admits it.\n13. A role the user does not hold is simply absent from the set. An unknown role NAME is refused at the\n   write door rather than admitted into the set and guessed at on read.\n\n--- END ---\n\nP1 DELIVERABLE: a working spec at `.adlc/specs/36-02-a-user-is-a-fact.md`. EVERY criterion above must name its\nverification — a test path or a backtick command — or `spec-lint` fails it as a wish. The plan itself\nfails spec-lint with 110 wishes ON PURPOSE (plan §9b): it is a decomposition, and these fifteen working\nspecs are the gated artifacts. Transcription, not invention.\n",
  "edges": [
    {
      "contract": "Phase 3 files a role binding and a key grant. Phase 2 defines what a role binding IS and how it resolves, so phase 3 has nothing to write until phase 2 lands.",
      "to": "T124"
    },
    {
      "contract": "A session row names a user. Phase 2 makes a user a readable fact, so the table has something to reference.",
      "to": "T125"
    }
  ],
  "id": "T123",
  "phase": "P3",
  "rails": [
    "test/server/users-ground.test.ts"
  ],
  "scope": [
    "src/server/users.ts",
    "src/gateway/gather.ts",
    "test/server/users-ground.test.ts",
    ".adlc/specs/36-02-a-user-is-a-fact.md"
  ],
  "status": "in-progress",
  "title": "§36 phase 2/15 — A user is a fact"
}
