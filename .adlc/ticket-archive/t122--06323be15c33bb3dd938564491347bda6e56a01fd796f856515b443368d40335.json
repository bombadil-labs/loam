{
  "_adlcArchive": {
    "archivedAt": "2026-07-28T10:18:52.046Z",
    "reason": "completed",
    "sourceRevision": null,
    "sourceStoreHash": "4d2c54636621006ea788b334f6ea9a5580ed546f8fabc17abd7b94f62b8894a3",
    "ticketHash": "37d35bf88ccc805c7880a6804e027b43bddda56cba2aec324e3a43b2c7ac92ab",
    "version": 1
  },
  "body": "PHASE 1 OF FIFTEEN. The plan is `.adlc/specs/users-oauth-phasing-plan.md`, approved by Myk\n2026-07-27. It replaces #282, which held 13,299 lines in one pull request that Myk could not read.\n\nTHIS TICKET IS ONE PULL REQUEST TO `main`. It merges on its own and it is useful on its own.\n\nWHO MERGES IT: SELF-MERGE on the ordinary bar: `npm run check` green, P5 clean, post-work audit clean. It implements a decision Myk already settled. Plan §9c.\n\nSPEC SECTION: §36. Per the plan's §9d, fifteen phases group into two sections. The FIRST phase of a\ngroup writes `spec/NN-slug.md`; every later phase EDITS it. Add one `{says, spec, proof}` claim to\nthis group's chapter in `demos/capabilities/chapters.mjs`, or `test/site/capabilities.test.ts` stays red.\n\nRAILS ARE EMPTY BY DESIGN — read the plan's §9e. CLAUDE.md says declare rails when the tests EXIST,\nnever in advance. A rail declared early binds two tickets together, which is exactly how #282 became one\nchange. Declare this ticket's rails at P3, once the tests are written and RED, using\n`adlc ticket update`. The rail files named below are SCOPE.\n\nTHE TWO RULES THIS PHASE OBEYS, from the plan's §1:\n  (i) it owns its own rail FILES and shares none with another phase;\n  (ii) it adds no PRECONDITION to a door an earlier phase railed.\n\nTHE SETTLED DECISIONS behind this work, so no phase reopens one:\n  - Role assignment is CLI-ONLY (Myk, 2026-07-27). The filesystem is the trust root; anyone who can read\n    `<home>/operator.seed` can already sign as the store. Plan §9.\n  - EVERY OPERATOR IS EQUIVALENT over the whole store (Myk, 2026-07-27). There are no operator tiers.\n    Per-operator keys buy ATTRIBUTION, never privilege separation. Plan §9a.\n  - `remove-role` binds against a REMOTE user only. It does not bind against home access. Do not claim\n    otherwise in any message or help text — that is the H7 shape. Plan §9a.\n  - Roles are a SET of non-negated claims, never last-write-wins (Myk).\n  - The login limiter DELAYS; it never denies and never locks (Myk, option b).\n\nWHAT THE REVIEW ROUNDS TAUGHT, and every criterion below carries one of these lessons rather than\nrediscovering it on round six. The full list is the plan's §2:\n  - The defect sits in the ADJACENT line. Review a fix by reading what moved beside it.\n  - A rail written from a model agrees with BOTH versions. Revert the fix and watch the rail fail.\n  - A rail of only negatives passes on an unrelated answer. Every negative needs a positive control.\n  - An instrument that cannot fail is worse than a rail that cannot fail. Make a probe report a\n    KNOWN-wrong answer before trusting it.\n  - Grep-verify any claim that names a file, a marker, a count or a command output.\n\n--- THE PHASE, VERBATIM FROM THE PLAN ---\n\n### Phase 1 — Credentials at rest\n\n**Verification.** Every criterion below is proved in `test/server/credentials.test.ts`. A criterion\nthat cannot name an assertion in that file is not ready to build.\n\n**Delivers.** `credentials.json` in the home. Per-user scrypt parameters, salt and hash. Atomic\nwrite. Mode 0600. No doors. No CLI.\n\n**Merges alone.** The credential primitive is testable as a library. Nothing reads it yet.\n\n**Must not.** It must not add a door. It must not add a CLI command.\n\n**Criteria from findings.**\n1. The salt reaches the derivation. Two hashes of one password differ. A hash crossed with another\n   salt does not verify. (A round trip through hash and verify passes even when `derive` ignores its\n   salt.)\n2. The stored bytes equal `scryptSync` over that entry's own salt and parameters. Assert against an\n   independent call, not against the pair under test.\n3. An entry records the parameters it was created with. Pin against the literal, never against the\n   file's own answer.\n4. Every shape of damage refuses, by name: truncated, not JSON, empty, wrong version, empty hash,\n   empty salt, non-hex hash, no parameters, unknown kind. \"Cannot determine\" never resolves to\n   \"matched\".\n5. The write is temp-then-rename. The inode changes. No residue remains. A stale temp does not\n   poison the result.\n6. The file lands at 0600 even when the path already sat at 0644.\n7. The mode is asserted on POSIX only. Windows reports 0666 whatever `chmod` asked. Name that gap in\n   the test file. State that a Windows run proves nothing about who may read a credential.\n8. `fsync` is asserted by nothing. Name that gap. An ESM named import offers no spy point.\n\n--- END ---\n\nP1 DELIVERABLE: a working spec at `.adlc/specs/36-01-credentials-at-rest.md`. EVERY criterion above must name its\nverification — a test path or a backtick command — or `spec-lint` fails it as a wish. The plan itself\nfails spec-lint with 110 wishes ON PURPOSE (plan §9b): it is a decomposition, and these fifteen working\nspecs are the gated artifacts. Transcription, not invention.\n",
  "edges": [
    {
      "contract": "Phase 3's `create` writes the credential phase 1 defines. The credential file's shape and its 0600 helper must exist before a command writes one, or phase 3 invents a second helper beside phase 1's.",
      "to": "T124"
    }
  ],
  "id": "T122",
  "phase": "P3",
  "rails": [
    "test/server/credentials.test.ts"
  ],
  "scope": [
    "src/server/credentials.ts",
    "test/server/credentials.test.ts",
    ".adlc/specs/36-01-credentials-at-rest.md"
  ],
  "status": "todo",
  "title": "§36 phase 1/15 — Credentials at rest"
}
