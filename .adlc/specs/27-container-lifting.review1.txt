  [33mMEDIUM  [39m [35m[concurrency][39m [1mCycle refusal is railed only for the initial attach; re-declaration (latest-wins) and reopen-from-ground can introduce or carry a cycle unchecked[22m  [2mconf 0.60[22m
    [36m.adlc/specs/27-container-lifting.md:48-51[39m
    The tree rule refuses a cycle 'at attach — the moment a declaration with a `parent` lands, or a live container is opened.' But `parent` is a living, latest-wins declaration (criterion 3: 'a knob change is a delta, not a restart'), so a cycle is far more likely to appear by RE-DECLARING an existing container's parent than during the first A→B→C→A build that criterion 8 tests. Criterion 8 only exercises the initial-attach sequence. Nothing rails (a) re-declaring C's parent to A on a running gateway, or (b) reopening a store whose ground already resolves to a cyclic parent table — the very 'hang at read time' the rule promises to prevent, now reachable through the two live paths the ticket itself introduces.
    [33m✗ failure:[39m Operator declares A→B, B→C, C=root; later re-declares C→A (a lawful knob change, criterion 3). If the cycle walk runs only on first attach, the running table now contains A→B→C→A. §28.6's host-flatten walks the parent chain and hangs at the next scoped read — exactly the outage the refusal was meant to make impossible.
    [32m→ fix:[39m Extend the cycle check (and criterion 8) to cover re-declaration of an existing container's `parent` on a running gateway, and to cover reopen-from-ground resolving a cyclic table, asserting the refusal/repair fires on those paths rather than only first-attach.

  [33mMEDIUM  [39m [35m[other][39m [1m'active containers' in the scope-merge formula is never defined[22m  [2mconf 0.60[22m
    [36m.adlc/specs/27-container-lifting.md:62-64[39m
    Scope-merge is specified as `union(active containers) MINUS excluded`, and it governs what every scope-bearing read resolves. But the artifact never defines which containers are 'active': whether a detached container counts, whether a child container is auto-included with its parent, whether an excluded-but-still-declared container is in the union, or how activation is even expressed at rest. This is the central read-time semantic and it is left to interpretation.
    [33m✗ failure:[39m Two engineers implement 'active': one treats every declared, non-detached container as active; the other requires an explicit open/mount and excludes children. The same store then resolves different member sets for the same `select`, and neither can be called wrong from the spec — a silent correctness divergence in the core read path.
    [32m→ fix:[39m Define the 'active' set precisely — how a container becomes active, whether detached/child/excluded containers participate — and add an acceptance criterion pinning the union membership, not just the difference (criteria 4/7 test exclusion, none test what 'active' includes).

  [33mMEDIUM  [39m [35m[error-handling][39m [1mMembership resolution failure (content-addressed Term unavailable) is unspecified for scoped reads[22m  [2mconf 0.55[22m
    [36m.adlc/specs/27-container-lifting.md:37-39[39m
    Membership 'references it — inline for small scopes, by content address for published ones', and every scoped read depends on evaluating that Term. The artifact never states what a scoped read returns when a content-addressed membership Term cannot be resolved — under a partial-federation fetch, a missing address, or a degraded backend. Silence here means the failure mode is left to each implementer.
    [33m✗ failure:[39m A published container's membership Term is content-addressed but the address is temporarily unreachable. One implementation fails the whole `select` closed; another resolves it to the empty set, silently dropping every member of that container from the union — a scoped read that quietly returns partial data with no error, indistinguishable from a legitimately empty container.
    [32m→ fix:[39m Specify the degraded-dependency behavior: whether an unresolvable membership Term fails the read closed or yields empty, and add a criterion asserting it, so partial federation cannot silently shrink a scoped result.

  [90mLOW     [39m [35m[privacy][39m [1mDetach record persists a caller-supplied note naming the container permanently, with no validation and no reconciliation for a lost store[22m  [2mconf 0.50[22m
    [36m.adlc/specs/27-container-lifting.md:41-45[39m
    `detach()` lands an operator-signed claim naming the container entity plus a 'caller-supplied note', and a reader can thereafter LIST detached stores. The note is unvalidated and, being a claim, persists at rest indefinitely. For a quarantine container (whose defining purpose is discard-with-zero-trace and whose data may be subject to §24.8 erasure), a permanent record naming it plus a free-text note is a metadata-retention surface the erasure discussion never reconciles. There is also no specified path when the surviving store is truly lost — the detached claim then dangles with no lawful negation ever possible.
    [33m✗ failure:[39m A quarantine container holding data slated for erasure is detached; the caller puts a filesystem path or descriptive note in the note field. The `loam.container.detached` claim persists forever and is enumerable via the listing reader, retaining identifying metadata about erased/quarantined content that the erasure law was meant to remove — and if the external store is later deleted, the dangling 'detached' record can never be negated by reattach.
    [32m→ fix:[39m State the note's validation/size constraints and its interaction with §24.8 erasure (can a detached record be erased?), and specify a reconciliation/negation path for a permanently-lost store so detached records cannot dangle forever.

[1mNext steps[22m
  • Resolve the trust/posture/copy model: enumerate all four trust×posture outcomes and reconcile the posture:wall default with criterion 6's zero-copy rule.
  • Either mint at-rest roles for `seeding` and `boundary` or explicitly scope them to the frozen quarantine preset for this ticket.
  • Extend cycle enforcement and criterion 8 to the re-declaration and reopen-from-ground paths.
  • Define the 'active containers' set and add a criterion pinning union membership, plus a criterion for unresolvable membership Terms.

