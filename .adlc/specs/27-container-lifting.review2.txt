    [32m→ fix:[39m Add to criterion 9 (or a new criterion) the H4 shape: two detaches accrue two records, one reattach negates BOTH, and the detached listing is empty afterward — asserted at the ground level (both negations present) and the listing level (container absent).

  [33mMEDIUM  [39m [35m[correctness][39m [1mTrust/posture immutability is enforced only at the door; the analogous tree rule is dual-level, leaving a federation/replay hole[22m  [2mconf 0.60[22m
    [36m.adlc/specs/27-container-lifting.md:49-55[39m
    The artifact enforces trust/posture immutability by refusing a changing re-declaration "at the door." For the structurally identical containment-cycle problem the artifact is careful to enforce at BOTH levels because "a cycle has two arrival paths and only one passes a door" — federation (two devices unioned) or a replayed ground. The same two arrival paths exist for an unlawful trust or posture flip: a declaration that would flip `untrusted`→`curated` can sit in a replayed ground or arrive federated from another of the operator's own devices, bypassing the local door. The reader has a cycle-guard but no stated immutability-guard, so latest-wins would resolve the flipped trust/posture. "Only lawful claims bind" filters STRANGERS, not the operator's own device-B re-declaration. Confidence is moderate because it depends on whether such a delta can reach a reader without ever passing a door — but the artifact itself asserts that path exists for cycles and does not close it for trust/posture.
    [33m✗ failure:[39m Operator's device B holds a ground that (via replay from before this rule, or a crafted/older delta) contains a `loam.container` re-declaration flipping a container from `untrusted` to `curated`. It federates to device A. A's door never evaluated it. A's reader applies latest-wins with no immutability check and resolves the container as `curated`/property — admitting walled content into shared-ground scope.
    [32m→ fix:[39m Mirror the tree rule: enforce trust/posture immutability at the READER too — a re-declaration that would change a bound container's trust or posture resolves as not-binding and is surfaced as a defect, not silently applied latest-wins. Add a criterion in the federation/replay shape (parallel to criterion 16).

  [33mMEDIUM  [39m [35m[correctness][39m [1mErasing the detach record contradicts the erasure-completeness guard[22m  [2mconf 0.65[22m
    [36m.adlc/specs/27-container-lifting.md:77-82[39m
    The detach bullet says an operator who must forget a quarantine existed "erases its record — the listing then honestly forgets." But the completeness guard refuses erase-completeness while an untrusted container is "neither currently attached nor covered by a surviving detached record." For an unreachable/detached untrusted wall, the detach record is the ONLY thing keeping `erase` from refusing. Erasing that record (while the container declaration still stands, unstruck) removes the coverage and flips `erase` back to permanently refusing completeness — the opposite of "honestly forgets." The two rules interact and the artifact never reconciles them: it does not say the container DECLARATION must also be struck, nor that erasing the record is only lawful once the wall is reachable/purged.
    [33m✗ failure:[39m An operator erases a detached untrusted wall's `loam.container.detached` record to forget the quarantine existed, but leaves (or cannot reach to strike) the standing `loam.container` declaration. The resolved container table still names the untrusted container; it is now neither attached nor covered. Every subsequent `erase` refuses completeness, naming a container the operator was told they had forgotten.
    [32m→ fix:[39m Specify the ordering: forgetting a quarantine must strike the container DECLARATION (removing it from the resolved table) as well as the detach record, or state that erasing the detach record of a still-declared unreachable untrusted container is itself the named fault. Add a criterion covering the record-erased-declaration-standing state.

  [90mLOW     [39m [35m[error-handling][39m [1mMalformed membership (both roles, or neither) is unspecified and unrailed[22m  [2mconf 0.60[22m
    [36m.adlc/specs/27-container-lifting.md:56-59[39m
    The membership bullet says the declaration references the Term "under TWO DISTINCT ROLES … One or the other, never both." The artifact does not say what the door does when a declaration carries BOTH `membership` and `membershipAt`, or NEITHER. Criterion 2 tests only NUL and prefix; criterion 19 tests an unresolvable `membershipAt` but not a structurally malformed declaration. Two implementers diverge: one refuses, one silently prefers `membershipAt`, one treats a no-membership declaration as an empty container (the H9 silent-shrink shape criterion 19 was written to prevent, arriving through a different door).
    [33m✗ failure:[39m A declaration carries both `membership` (inline JSON) and `membershipAt` (an address) that disagree. The implementation silently picks one; a scope-read resolves a member set the operator did not intend, and no rail catches the ambiguity because "never both" is stated as a rule with no enforcement clause.
    [32m→ fix:[39m State the door's behavior for both-provided and neither-provided declarations (refuse, naming the malformation) and add it to the vocabulary rail in criterion 2.

[1mNext steps[22m
  • Specify the `trust` knob's absent-rule (required, or a stated safe default) and rail it — the single most load-bearing gap.
  • Reconcile the erasure-completeness guard's `untrusted`-only scope with the byte copies criterion 5 puts in curated walls; widen the guard or justify the exemption with a criterion.
  • Add acceptance criteria for the H4 double-detach negation, reader-level trust/posture immutability under federation/replay, the detach-record-vs-completeness interaction, and malformed membership declarations.

