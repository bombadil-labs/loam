  [31mHIGH    [39m [35m[concurrency][39m [1mAtomic compare-and-swap refusal asserts a behavior with no named substrate mechanism, and may be unenforceable on an append-only log[22m  [2mconf 0.55[22m
    [36m.adlc/specs/27-trust-on-load.md:62-69[39m
    The guard is required to be atomic: the adoption 'carries the winner it observed' and 'the publish REFUSES if the winner moved in between.' On a content-addressed, append-only delta store where §21 'latest-wins' is a READ-TIME resolution over all registrations, two concurrent publishes both succeed as deltas — there is no write-time point at which one 'refuses.' A conditional/optimistic-concurrency write is a genuine primitive; the spec both demands it (AC14) and elsewhere insists 'No new trust machinery: the blessing IS the publish the operator could already perform.' Whether the ordinary publish path can refuse conditionally, and where adoptions serialize, is never identified — and rhizomatic is frozen, so a new lock primitive cannot simply be added. This is an inference about the substrate from Loam's append-only nature; if a single-writer gateway or an existing optimistic-write primitive provides the serialization point, the finding is resolved by naming it — but as written the requirement floats free of any mechanism.
    [33m✗ failure:[39m Two adoptions of different-content law under the same semantic name are processed by the write path; both were appended as valid registrations because the append-only store has no conditional write. Latest-wins resolves to whichever landed second at read time — un-reviewed — reproducing exactly the timing-door confused-deputy the atomicity clause was written to eliminate, while AC14 was believed to cover it.
    [32m→ fix:[39m Name the serialization mechanism the CAS relies on (e.g. a single-writer gateway per root, or a specific rhizomatic optimistic-write/precondition primitive with a citation). If no such primitive exists on the frozen substrate, mark AC14 as blocked on a rhizomatic conversation rather than implying it is buildable with 'no new machinery.'

  [33mMEDIUM  [39m [35m[test-integrity][39m [1mThe concurrency acceptance criterion (AC14) names no deterministic way to force the race, so its rail can pass vacuously[22m  [2mconf 0.60[22m
    [36m.adlc/specs/27-trust-on-load.md:168-171[39m
    AC14 requires proving that 'two interleaved adoptions ... the second beginning after the first's check, before its publish' resolve to one success and one refusal. Its only named verification is the same test file as every other criterion, with no mention of a scheduling seam, injected barrier, or hook that deterministically reproduces the check-then-publish interleaving. A test that cannot force the window will run the two adoptions sequentially, observe one success and one refusal, and pass — even if the underlying publish path performs NO atomic refusal at all. This is precisely the hollow-rail hazard the process elsewhere obsesses over: the atomicity claim's rail could pass with the atomicity absent.
    [33m✗ failure:[39m An implementer builds check-then-publish with no CAS. AC14's test runs the two adoptions without a forced interleaving; the second happens to observe the first's completed registration and refuses; the test goes green. The race remains live in production, but the rail certified it closed.
    [32m→ fix:[39m Have AC14 name the mechanism that deterministically reproduces the interleaving (an injected seam/barrier between observe and publish, or a fault-injection hook), so the test provably exercises the window and cannot pass without a real atomic refusal.

  [33mMEDIUM  [39m [35m[resource-exhaustion][39m [1mIdempotent re-adoption mints a new witness record every time, with no dedup bound stated[22m  [2mconf 0.50[22m
    [36m.adlc/specs/27-trust-on-load.md:98-100[39m
    'Adopting a row whose content address is already blessed is a no-op — recorded as a DISTINGUISHABLE record kind, witnessed.' The spec later concedes 'witnesses accumulate,' but treats accumulation only as a lawFrom-accuracy nuisance, not as unbounded growth from repeated idempotent calls. Nothing states that a repeated witness of the SAME (module, row, address) is deduped or suppressed. A routine that re-runs blessAll on a schedule (a common operational pattern for 'keep my adoptions current') would therefore append a fresh narrative record on every pass, forever.
    [33m✗ failure:[39m An operator schedules an hourly blessAll to pick up new rows on version bumps. Every run re-witnesses all already-blessed rows, appending N narrative records per hour indefinitely — thousands of redundant witness records per row per year, bloating the ledger and degrading every provenance/lawFrom walk that scans them.
    [32m→ fix:[39m State whether a redundant witness of an identical (source, alias, content-address) tuple is deduped/suppressed rather than re-minted, or otherwise bound the growth. Add an acceptance criterion that re-running an idempotent adoption twice does not accumulate a second witness record.

  [90mLOW     [39m [35m[error-handling][39m [1mblessAll enumeration over a malformed manifest (dangling or unresolvable rows, empty manifest) is unspecified[22m  [2mconf 0.50[22m
    [36m.adlc/specs/27-trust-on-load.md (file-level)[39m
    blessAll 'enumerates the manifest's law rows,' and classification is correctly read from the verified export bytes rather than manifest labels (AC15). But the module is a stranger's, and its manifest may list a row whose content address resolves to nothing (dangling), be empty, or point at bytes that are not a valid binding of any kind. The spec addresses a manifest that MISLABELS a real export (AC15) but not a manifest whose rows are unresolvable or absent. Enumeration behavior on these inputs — skip loudly, refuse the whole call, or ignore silently — is undefined, and 'ignore silently' would let a crafted manifest hide rows or produce a confusing partial adoption.
    [33m✗ failure:[39m A malicious module ships a manifest whose pen-holding renderer row points at an address that resolves only after a later lazy fetch, or lists rows out of sync with its actual exports. blessAll enumerates the manifest, hits an unresolvable/edge row, and — with no specified behavior — either crashes mid-adoption (partial state, see the rollback finding) or silently skips, defeating the operator's expectation of a complete wholesale bless.
    [32m→ fix:[39m Specify enumeration behavior for empty manifests and for rows whose content address does not resolve to a valid, classifiable export: refuse the call naming the offending row, and add it to the acceptance criteria alongside the lying-manifest case (AC15).

[1mNext steps[22m
  • Decide and document, per law kind (schema/resolver/renderer), whether the root-name guard applies and add mirroring acceptance criteria for resolver and renderer collisions.
  • Name the substrate serialization/optimistic-write mechanism the atomic CAS depends on, or mark AC14 blocked on a rhizomatic conversation instead of 'no new machinery.'
  • Give AC14 an explicit interleaving-injection mechanism so the atomicity rail cannot pass sequentially.
  • Specify the partial-failure recovery/resume contract for blessAll and the dedup bound for repeated witness records.

