{
  "//": [
    "Authorized vocabulary renames inside FROZEN rails \u2014 read by scripts/rails-guard-ci.mjs.",
    "",
    "THE GATE READS THIS FILE FROM THE BASE TREE, NEVER FROM THE BRANCH. That is the entire",
    "security model: a branch cannot self-authorize, because the entry that exempts its edit",
    "must already be on main \u2014 i.e. it arrived through a PR a human reviewed. Two-PR",
    "discipline, enforced mechanically (Myk, 2026-07-26): first land the authorization here,",
    "then open the rename.",
    "",
    "An entry exempts a frozen-rail edit ONLY when base + the applicable substitutions is",
    "byte-identical to the branch's file (directly, or after the repo's own prettier). Any",
    "edit beyond the declared substitution is a violation, same as ever.",
    "",
    "Shape: { from, to, files?, authorized }. `files` limits the pair to exact paths; an",
    "UNSCOPED pair applies to every frozen rail and must be word-shaped (identifier or quoted",
    "literal) \u2014 a SCOPED pair may carry wider literals (a regex fragment, a title phrase)",
    "because the reviewer reads it against the named files. `authorized` names who and when.",
    "Remove entries once the rename that consumed them has merged; a spent entry is inert",
    "(the substitution no longer produces the base) but tidiness keeps this file reviewable."
  ],
  "renames": [],
  "revisions": [
    {
      "file": "test/federation/arrival-attestations.test.ts",
      "sha256": "0a99c8bd44fa2636ec2fde6550ec520eaf520f1cc8f2d4c1d5a789e39b609123",
      "authorized": "PENDING MYK - merging this entry IS the authorization; the settled SPEC 49 pulse law is the ruling it records.",
      "//": "T212 revisions - SPEC 49 position 1 (the pulse law, settled by Myk 2026-08-28) supersedes the per-poll channel stamp these four rails pinned: a quiet sync now writes NOTHING (the audit rail test/federation/pulse-law-audit.test.ts found the violation on its first run). arrival-attestations: the quiet-poll clock holds instead of advancing. crash-window (b): a quiet sync adds zero records, not one. stale-handle-restamp: the bystander completes quietly instead of stamping, and the header says so; the sever refusals themselves are UNCHANGED and the quiet path re-reads liveness (red-proven by the existing T233(c) case). channel-record-trust: the human label moves to 'last recorded sync' because lastSyncedAt now names the last EVENTFUL sync - an independent suppression lens flagged the old label as overclaiming. Every edit was lens-reviewed; the full federation suite is 243/243 on the branch. REMOVE all four entries once the T212 build PR merges."
    },
    {
      "file": "test/federation/crash-window.test.ts",
      "sha256": "d879caf7c4ce07887b9d9941571d5d9e4341cc51120ff02c378e8943b2cb4f94",
      "authorized": "PENDING MYK - merging this entry IS the authorization; the settled SPEC 49 pulse law is the ruling it records."
    },
    {
      "file": "test/federation/stale-handle-restamp.test.ts",
      "sha256": "7c0d1cf4df66f96a9fc0ffe15df5ca8ca7651a41cdb2b9c0ccb5e1b3ffe25dbb",
      "authorized": "PENDING MYK - merging this entry IS the authorization; the settled SPEC 49 pulse law is the ruling it records."
    },
    {
      "file": "test/federation/channel-record-trust.test.ts",
      "sha256": "6b39605d0d4b02dcdbba873fe6d14e8ab835aa37777d2b9aeee9d2c1fb6d0714",
      "authorized": "PENDING MYK - merging this entry IS the authorization; the settled SPEC 49 pulse law is the ruling it records."
    }
  ]
}
