{
  "//": [
    "Authorized vocabulary renames inside FROZEN rails \u2014 read by scripts/rails-guard-ci.mjs.",
    "",
    "THE GATE READS THIS FILE FROM THE BASE TREE, NEVER FROM THE BRANCH. That is the entire",
    "security model: a branch cannot self-authorize, because the entry that exempts its edit",
    "must already be on main \u2014 i.e. it arrived through a PR a human reviewed. Two-PR",
    "discipline, enforced mechanically (Myk, 2026-07-26): first land the authorization here,",
    "then open the rename.",
    "",
    "An entry exempts a frozen-rail edit ONLY when base + the applicable substitutions is",
    "byte-identical to the branch's file (directly, or after the repo's own prettier). Any",
    "edit beyond the declared substitution is a violation, same as ever.",
    "",
    "Shape: { from, to, files?, authorized }. `files` limits the pair to exact paths; an",
    "UNSCOPED pair applies to every frozen rail and must be word-shaped (identifier or quoted",
    "literal) \u2014 a SCOPED pair may carry wider literals (a regex fragment, a title phrase)",
    "because the reviewer reads it against the named files. `authorized` names who and when.",
    "Remove entries once the rename that consumed them has merged; a spent entry is inert",
    "(the substitution no longer produces the base) but tidiness keeps this file reviewable."
  ],
  "renames": [
    {
      "from": "    // `toContain`, not `toBe`: a pool's 200 is wrapped in \u00a724.7's probation chrome, which is a\n    // different promise and belongs to its own rails.\n    expect((await pool.gateway.serveRoute(\"quick\", FERN, \"full\")).body).toContain(\n      \"<p>height: 42</p>\",\n    );\n    await pool.drop();\n    await gw.close();\n  }, 30_000);\n",
      "to": "    // The serve below runs under a WIDENED envelope, appended as a delta (a widening is a delta,\n    // not a restart). \u00a724.5 charges worker SPAWN to the pool's declared clock \u2014 the envelope is the\n    // pool's whole bill \u2014 so a serve asserted under the tight 100ms bill races host scheduling: on\n    // a loaded box spawn alone overruns it and the pool refuses while keeping its promise. The\n    // admission assertions above stay on the tight ceiling, where the clock bounds the module body\n    // and spawn keeps \u00a723.9's host-sized window; only the render needs a bill the host can meet.\n    await gw.append([\n      signClaims(envelopeClaims(ENVELOPE_ANY, { renderTimeoutMs: 10_000 }, OP, 4000), OP_SEED),\n    ]);\n    expect(gw.envelopeReports()[0]!.envelope.renderTimeoutMs).toBe(10_000);\n    await pool.gateway.prepareRoute(\"quick\");\n    // `toContain`, not `toBe`: a pool's 200 is wrapped in \u00a724.7's probation chrome, which is a\n    // different promise and belongs to its own rails.\n    expect((await pool.gateway.serveRoute(\"quick\", FERN, \"full\")).body).toContain(\n      \"<p>height: 42</p>\",\n    );\n    await pool.drop();\n    await gw.close();\n  }, 30_000);\n",
      "files": ["test/gateway/render-ocap.test.ts"],
      "authorized": "PENDING MYK - a branch cannot self-authorize, so merging this entry IS the authorization. One pair, one file; the serve half of one test moves to a widened envelope.",
      "//": "T248 triage: the T172 rail's two-sided serve asserts a pool render completes under a declared 100ms bill. renderers.ts charges worker SPAWN to that bill BY DESIGN (the envelope is the pool's whole bill), so on a loaded host spawn alone overruns 100ms and the pool refuses while keeping its promise -- the rail asserts more than the spec promises. Observed twice: windows CI on a JSON-only diff (run 33137867176, ubuntu green in the same run), and one loaded local full check on 2026-08-27. The pair keeps every tight-bill admission assertion byte-identical and moves ONLY the serve: a later envelope delta widens renderTimeoutMs to 10000 (asserted taken), prepareRoute re-admits under the new bill, then the serve runs with a host-sized spawn window. Nothing is deleted and no assertion widens; two assertions are ADDED. Measured: edited test 24/24 x8 under full-suite load, incl. pinned to 2 cores; original did not reproduce on linux in 3 pinned runs (both observed reds are the windows/full-check shape). REMOVE this pair once the edit merges."
    }
  ]
}
