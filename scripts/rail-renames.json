{
  "//": [
    "Authorized vocabulary renames inside FROZEN rails \u2014 read by scripts/rails-guard-ci.mjs.",
    "",
    "THE GATE READS THIS FILE FROM THE BASE TREE, NEVER FROM THE BRANCH. That is the entire",
    "security model: a branch cannot self-authorize, because the entry that exempts its edit",
    "must already be on main \u2014 i.e. it arrived through a PR a human reviewed. Two-PR",
    "discipline, enforced mechanically (Myk, 2026-07-26): first land the authorization here,",
    "then open the rename.",
    "",
    "An entry exempts a frozen-rail edit ONLY when base + the applicable substitutions is",
    "byte-identical to the branch's file (directly, or after the repo's own prettier). Any",
    "edit beyond the declared substitution is a violation, same as ever.",
    "",
    "Shape: { from, to, files?, authorized }. `files` limits the pair to exact paths; an",
    "UNSCOPED pair applies to every frozen rail and must be word-shaped (identifier or quoted",
    "literal) \u2014 a SCOPED pair may carry wider literals (a regex fragment, a title phrase)",
    "because the reviewer reads it against the named files. `authorized` names who and when.",
    "Remove entries once the rename that consumed them has merged; a spent entry is inert",
    "(the substitution no longer produces the base) but tidiness keeps this file reviewable."
  ],
  "renames": [
    {
      "from": "    const many = Array.from({ length: 200 }, (_, i) =>",
      "to": "    const many = Array.from({ length: 60 }, (_, i) =>",
      "files": ["test/store/archive.test.ts"],
      "authorized": "PENDING MYK - a branch cannot self-authorize, so merging this entry IS the authorization. Fixture size only; both probes verified red.",
      "//": "T180 rail WEIGHT reduction, 6 pairs, one file. Measured 2026-08-17 on a ticket-only PR (#420), so the failure cannot be a regression from content: the purge-sweep case took 511ms on linux and exceeded the 20000ms per-test ceiling on windows, where the whole file ran 22317ms for 8 tests. The Defender hypothesis is REFUTED -- the failing run's log shows both exclusions succeeding, so the cost is windows file creation and fsync on a loaded runner. T155 pooled the fsyncs and T159 already raised this file from 15s to 20s citing it at 20.1s; raising the ceiling a third time is the move that has been made twice. The property under test is that purge sweeps FILES rather than walking id-by-fan and counts DISTINCT IDS ACTUALLY FOUND. Its three populations and their RATIOS carry that proof; the MAGNITUDE does not, because nothing asserts a duration or a pass count. So 200 appended becomes 60, 30 doomed of which 15 carry a crash-left straggler, 15 never present. PROVEN STILL RED, both directions, against the smaller numbers: counting requested ids instead of found ids answers 'expected 45 to be 30', and refusing to match .tmp stragglers leaves 15 behind and reddens the directory sweep. No assertion is weakened and no timeout moves -- only the fixture's size. REMOVE these pairs once the edit merges."
    },
    {
      "from": "    const doomed = many.slice(0, 100);",
      "to": "    const doomed = many.slice(0, 30);",
      "files": ["test/store/archive.test.ts"],
      "authorized": "PENDING MYK - a branch cannot self-authorize, so merging this entry IS the authorization. Fixture size only; both probes verified red."
    },
    {
      "from": "    for (const d of doomed.slice(0, 50)) {",
      "to": "    for (const d of doomed.slice(0, 15)) {",
      "files": ["test/store/archive.test.ts"],
      "authorized": "PENDING MYK - a branch cannot self-authorize, so merging this entry IS the authorization. Fixture size only; both probes verified red."
    },
    {
      "from": "    const neverHere = Array.from({ length: 50 }, (_, i) =>",
      "to": "    const neverHere = Array.from({ length: 15 }, (_, i) =>",
      "files": ["test/store/archive.test.ts"],
      "authorized": "PENDING MYK - a branch cannot self-authorize, so merging this entry IS the authorization. Fixture size only; both probes verified red."
    },
    {
      "from": "    expect(removed).toBe(100);",
      "to": "    expect(removed).toBe(30);",
      "files": ["test/store/archive.test.ts"],
      "authorized": "PENDING MYK - a branch cannot self-authorize, so merging this entry IS the authorization. Fixture size only; both probes verified red."
    },
    {
      "from": "    expect(left.length).toBe(100);",
      "to": "    expect(left.length).toBe(30);",
      "files": ["test/store/archive.test.ts"],
      "authorized": "PENDING MYK - a branch cannot self-authorize, so merging this entry IS the authorization. Fixture size only; both probes verified red."
    },
    {
      "from": "the second channel reports WITNESSED, never bound, because its name is not created",
      "to": "both names bind: identical law under two names is two bindings (was the T198 interim)",
      "files": ["test/federation/name-parked.test.ts"],
      "authorized": "Myk, 2026-08-20, in chat \u2014 T200/\u00a747 criterion 2: the (address, living name) witness makes both peers' names bind; the interim witnessed-only rail flips to assert the fixed behavior."
    },
    {
      "from": "whatever it is called, and adoptLaw refuses to publish law it already serves under another",
      "to": "whatever it is called. The witness now keys on (address, living name) per criterion 2, so",
      "files": ["test/federation/name-parked.test.ts"],
      "authorized": "Myk, 2026-08-20, in chat \u2014 T200/\u00a747 criterion 2: the (address, living name) witness makes both peers' names bind; the interim witnessed-only rail flips to assert the fixed behavior."
    },
    {
      "from": "deliberately, so a module blessed twice does not bind twice.",
      "to": "the same law under a NEW name publishes; a SAME-name repeat still witnesses.",
      "files": ["test/federation/name-parked.test.ts"],
      "authorized": "Myk, 2026-08-20, in chat \u2014 T200/\u00a747 criterion 2: the (address, living name) witness makes both peers' names bind; the interim witnessed-only rail flips to assert the fixed behavior."
    },
    {
      "from": "The defect was reporting that outcome as `bound`. The report said the name serves; the store",
      "to": "The interim (T198) reported `witnessed` honestly while the name stayed uncreated; this now",
      "files": ["test/federation/name-parked.test.ts"],
      "authorized": "Myk, 2026-08-20, in chat \u2014 T200/\u00a747 criterion 2: the (address, living name) witness makes both peers' names bind; the interim witnessed-only rail flips to assert the fixed behavior."
    },
    {
      "from": "threw on it. Until a second name can serve the same law, saying so is the honest answer.",
      "to": "asserts the fix, and identical-law-two-peers.test.ts proves both names SERVE real reads.",
      "files": ["test/federation/name-parked.test.ts"],
      "authorized": "Myk, 2026-08-20, in chat \u2014 T200/\u00a747 criterion 2: the (address, living name) witness makes both peers' names bind; the interim witnessed-only rail flips to assert the fixed behavior."
    },
    {
      "from": "expect(second.bound).not.toContain(\"bob:Plant\");",
      "to": "expect(second.bound).toContain(\"bob:Plant\");",
      "files": ["test/federation/name-parked.test.ts"],
      "authorized": "Myk, 2026-08-20, in chat \u2014 T200/\u00a747 criterion 2: the (address, living name) witness makes both peers' names bind; the interim witnessed-only rail flips to assert the fixed behavior."
    },
    {
      "from": "expect(second.witnessed).toContain(\"bob:Plant\");",
      "to": "expect(second.witnessed).toEqual([]);",
      "files": ["test/federation/name-parked.test.ts"],
      "authorized": "Myk, 2026-08-20, in chat \u2014 T200/\u00a747 criterion 2: the (address, living name) witness makes both peers' names bind; the interim witnessed-only rail flips to assert the fixed behavior."
    },
    {
      "from": "And the witnessed name is exactly the one that does not serve.",
      "to": "And the second name serves: def resolves it rather than throwing.",
      "files": ["test/federation/name-parked.test.ts"],
      "authorized": "Myk, 2026-08-20, in chat \u2014 T200/\u00a747 criterion 2: the (address, living name) witness makes both peers' names bind; the interim witnessed-only rail flips to assert the fixed behavior."
    },
    {
      "from": "expect(() => me.def(\"bob:Plant\")).toThrow();",
      "to": "expect(() => me.def(\"bob:Plant\")).not.toThrow();",
      "files": ["test/federation/name-parked.test.ts"],
      "authorized": "Myk, 2026-08-20, in chat \u2014 T200/\u00a747 criterion 2: the (address, living name) witness makes both peers' names bind; the interim witnessed-only rail flips to assert the fixed behavior."
    }
  ]
}
