{
  "//": [
    "Authorized vocabulary renames inside FROZEN rails \u2014 read by scripts/rails-guard-ci.mjs.",
    "",
    "THE GATE READS THIS FILE FROM THE BASE TREE, NEVER FROM THE BRANCH. That is the entire",
    "security model: a branch cannot self-authorize, because the entry that exempts its edit",
    "must already be on main \u2014 i.e. it arrived through a PR a human reviewed. Two-PR",
    "discipline, enforced mechanically (Myk, 2026-07-26): first land the authorization here,",
    "then open the rename.",
    "",
    "An entry exempts a frozen-rail edit ONLY when base + the applicable substitutions is",
    "byte-identical to the branch's file (directly, or after the repo's own prettier). Any",
    "edit beyond the declared substitution is a violation, same as ever.",
    "",
    "Shape: { from, to, files?, authorized }. `files` limits the pair to exact paths; an",
    "UNSCOPED pair applies to every frozen rail and must be word-shaped (identifier or quoted",
    "literal) \u2014 a SCOPED pair may carry wider literals (a regex fragment, a title phrase)",
    "because the reviewer reads it against the named files. `authorized` names who and when.",
    "Remove entries once the rename that consumed them has merged; a spent entry is inert",
    "(the substitution no longer produces the base) but tidiness keeps this file reviewable."
  ],
  "renames": [
    {
      "from": "import { grantClaims, holdsGrant } from \"../../src/gateway/accounts.js\";",
      "to": "import { grantClaims, holdsGrant } from \"../../src/gateway/accounts.js\"; PBBILL1",
      "files": ["test/gateway/probation-frame.test.ts"],
      "authorized": "PENDING MYK - merging this entry IS the authorization; T248's mechanism, third application.",
      "//": "T253: probation-frame joins the host-sized pool bill by three chained pairs (import via marker, then the bill call before primary()'s return, which publicPrimary shares by delegation). The mechanism is T248's, third application: a pool render charges worker SPAWN to the declared clock, so success assertions under the 500ms default race a loaded host - this file paid the flake on 2026-08-29 (500 for 200, 23/23 in isolation). No assertion changes; the fixture declares the 10s wildcard bill test/helpers/pool-bill.ts carries (landed in #507 with the t209-fixtures half - 57 install-by-federation rails already wear it, no ceremony needed there because t209-fixtures.ts is not a declared rail). Verified: base + pairs + prettier is byte-identical to the prepared branch file; 23/23 green billed. REMOVE all three pairs once the edit merges."
    },
    {
      "from": "PBBILL1",
      "to": "import { declareHostSizedBill } from \"../helpers/pool-bill.js\";",
      "files": ["test/gateway/probation-frame.test.ts"],
      "authorized": "PENDING MYK - merging this entry IS the authorization; T248's mechanism, third application."
    },
    {
      "from": "  return { gw, backend };",
      "to": "  await declareHostSizedBill(gw, 9_150); return { gw, backend };",
      "files": ["test/gateway/probation-frame.test.ts"],
      "authorized": "PENDING MYK - merging this entry IS the authorization; T248's mechanism, third application."
    }
  ],
  "revisions": []
}
