{
  "//": [
    "Authorized vocabulary renames inside FROZEN rails \u2014 read by scripts/rails-guard-ci.mjs.",
    "",
    "THE GATE READS THIS FILE FROM THE BASE TREE, NEVER FROM THE BRANCH. That is the entire",
    "security model: a branch cannot self-authorize, because the entry that exempts its edit",
    "must already be on main \u2014 i.e. it arrived through a PR a human reviewed. Two-PR",
    "discipline, enforced mechanically (Myk, 2026-07-26): first land the authorization here,",
    "then open the rename.",
    "",
    "An entry exempts a frozen-rail edit ONLY when base + the applicable substitutions is",
    "byte-identical to the branch's file (directly, or after the repo's own prettier). Any",
    "edit beyond the declared substitution is a violation, same as ever.",
    "",
    "Shape: { from, to, files?, authorized }. `files` limits the pair to exact paths; an",
    "UNSCOPED pair applies to every frozen rail and must be word-shaped (identifier or quoted",
    "literal) \u2014 a SCOPED pair may carry wider literals (a regex fragment, a title phrase)",
    "because the reviewer reads it against the named files. `authorized` names who and when.",
    "Remove entries once the rename that consumed them has merged; a spent entry is inert",
    "(the substitution no longer produces the base) but tidiness keeps this file reviewable."
  ],
  "renames": [
    {
      "from": "    expect((await pool.gateway.serveRoute(\"quick\", FERN, \"full\")).body).toContain(",
      "to": "    WIDENBILL expect((await pool.gateway.serveRoute(\"quick\", FERN, \"full\")).body).toContain(",
      "files": ["test/gateway/render-ocap.test.ts"],
      "authorized": "PENDING MYK - a branch cannot self-authorize; merging this entry IS the authorization (re-expression of #493, which the gate could not read).",
      "//": "T248, second declaration - #493's entry was multi-line with section marks, which the gate's own shape guard refuses to read (one line, printable ASCII, 120 chars); Myk authorized the edit there and this re-expresses it in the grammar the gate enforces. The edit: the T172 rail's two-sided serve asserted a pool render completes under a declared 100ms bill, but the pool charges worker SPAWN to that bill by design, so a loaded host refuses while the code keeps its promise (observed: windows CI run 33137867176 on a JSON-only diff, and one loaded local full check 2026-08-27). Four chained pairs prepend three statements to the serve, re-expanded by prettier: widen the envelope by a later delta, assert the widening took, re-admit under the new bill. Every tight-bill assertion stays byte-identical; nothing is deleted or widened; two assertions are ADDED. Measured: edited test 24/24 x9 under load incl. pinned to 2 cores. REMOVE all four pairs once the edit merges."
    },
    {
      "from": "WIDENBILL",
      "to": "await gw.append([signClaims(envelopeClaims(ENVELOPE_ANY, { renderTimeoutMs: 10_000 }, OP, 4000), OP_SEED)]); WIDENB2",
      "files": ["test/gateway/render-ocap.test.ts"],
      "authorized": "PENDING MYK - a branch cannot self-authorize; merging this entry IS the authorization (re-expression of #493, which the gate could not read)."
    },
    {
      "from": "WIDENB2",
      "to": "expect(gw.envelopeReports()[0]!.envelope.renderTimeoutMs).toBe(10_000); WIDENB3",
      "files": ["test/gateway/render-ocap.test.ts"],
      "authorized": "PENDING MYK - a branch cannot self-authorize; merging this entry IS the authorization (re-expression of #493, which the gate could not read)."
    },
    {
      "from": "WIDENB3",
      "to": "await pool.gateway.prepareRoute(\"quick\");",
      "files": ["test/gateway/render-ocap.test.ts"],
      "authorized": "PENDING MYK - a branch cannot self-authorize; merging this entry IS the authorization (re-expression of #493, which the gate could not read)."
    }
  ]
}
