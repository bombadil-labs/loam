# .gitignore already excludes dist/ and node_modules/; prettier 3 respects it.
package-lock.json
# Markdown docs are hand-formatted; leave them to the author.
*.md
# The ephemeral field-test playground (never committed).
demos/village/
# Generated tutorial packets: committed data, regenerated byte-identically by
# scripts/gen-packets.mjs (--check gates drift) — a formatter pass would break identity.
demos/tutorial/packets/
